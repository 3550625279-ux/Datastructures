# 教学版 List<T>：本周实践手册

## 0. 目标与停止条件——所有工作以此为准

> 我写这个教学版 `list`，不是为了记忆链表模板，而是为了学会从容器的 API 与复杂度契约反推内部表示，用明确的不变量推导和检查指针修改，并理解节点所有权、iterator 与容器之间的边界。

> 当我能脱离成品代码画出结构、陈述不变量、推导核心修改；能根据接口与不变量从零重建核心部分；能解释主要设计选择和复杂度；并用测试、结构断言及内存检查验证实现，同时清楚它与完整 `std::list` 的差距时，本周任务结束。

本周要带走的不是更多函数，而是三件事：

1. **设计理由**：某个需求为什么需要某种内部表示？哪些是必须条件，哪些只是我的选择？
2. **结构推导能力**：不看成品代码，也能从图和不变量推出插入、删除。
3. **容器边界意识**：元素由谁拥有，位置由谁记录，接口承诺什么，如何验证承诺。

执行方式：任务书 → 与 AI 讨论并写出自己的接口文档 → 分步实现 → AI 提供系统测试、我运行并修正 → 脱离代码验收。

**时间是资源上限，不是进度保证。** 周二 90 分钟、周三晚上 50 分钟、周四 90 分钟，共 230 分钟。若仍沿用每周最多 300 分钟，周末余量最多约 70 分钟。下面不把时段切成分钟；日期表示主攻方向，不要求未完成前一关就进入下一关。对你目前的水平，泛型、iterator 和调试都有学习成本，不能保证一定在 300 分钟内完成。到上限仍有缺口，就如实记录，不用“代码能跑”冒充闭环。

---

## 1. 任务书：我要交付什么

### 1.1 命题

实现一个教学版 C++ `List<T>`。它对外表示一个有顺序的元素序列，对内自行管理链接节点。调用者可以通过独立的位置对象遍历、插入和删除，不需要知道节点布局。

这里的“教学版”意味着明确取舍，不意味着可以泄漏内存、保留错误浅复制，或对外承诺做不到的复杂度。

### 1.2 必须满足的需求

| 编号 | 外部需求与语义 | 目标复杂度 | 对设计提出的问题 |
| --- | --- | --- | --- |
| R1 | 默认构造得到空序列；能查询 `empty` 和 `size` | O(1) | 空表如何表示？节点数量存在哪里？ |
| R2 | 非空时能访问首、尾元素，并能修改它们 | O(1) | 如何直接找到两端？返回副本还是引用？ |
| R3 | 有 `begin/end`；空表二者相等；支持前进、后退、解引用及比较 | 每步 O(1) | 尾后位置如何表示？向前一步需要什么信息？ |
| R4 | 多个 iterator 可以独立遍历；复制 iterator 不复制元素 | O(1) 复制位置 | 遍历状态应该放在容器还是 iterator 中？ |
| R5 | 在合法 `pos` 之前插入一个元素，返回新元素位置；允许 `pos == end()` | O(1) 链接工作 | 如何获得前驱？怎样保持旧位置有效？ |
| R6 | 删除合法且可解引用的 `pos`，返回其原后继位置 | O(1) 链接工作 | 如何重新连接两侧？什么时候释放节点？ |
| R7 | 头插、尾插复用通用插入的结构逻辑 | O(1) 链接工作 | 能否用少量核心操作支撑其他接口？ |
| R8 | `clear` 后恢复空表；析构释放全部真实节点 | O(n) | 谁拥有节点？删除最后一个后如何恢复空表？ |
| R9 | 插入保留已有元素的 iterator/引用；删除只使被删元素的 iterator/引用失效 | 不另加遍历 | 是否真正维持节点身份，而非搬动其他元素？ |
| R10 | 空 `List<T>` 不要求 `T` 可默认构造；插入支持从 `const T&` 复制构造元素 | 见下文 | 哨兵是否真的应该构造一个 T？ |
| R11 | 显式禁止本周不实现的容器复制、复制赋值、移动、移动赋值 | 编译期限制 | 默认复制内部指针为何不等于复制容器？ |
| R12 | 插入时若分配或元素构造失败，原序列、数量、链接保持不变且无泄漏 | 不做完整异常体系 | 能否先成功创建节点，再提交结构修改？ |

复杂度以元素数 n 为变量。单元素插入、删除的链接工作不随 n 增长，另包含一次元素构造或析构及相应内存管理成本；不承诺任意 T 的操作都耗时相同。先查找第 k 个位置再插入，整体通常是 O(n)，不能把查找成本省略。

这组需求参考了标准 `list` 的双向遍历、单元素插入删除复杂度及位置稳定性，但本项目不宣称完整标准兼容。参见 [C++ 工作草案：list 概述](https://eel.is/c++draft/list.overview)、[list 修改操作](https://eel.is/c++draft/list.modifiers)。

### 1.3 必须写清的合法操作边界

- `insert` 的位置必须来自当前容器且仍有效，可以是它的 `end()`。
- `erase` 的位置必须来自当前容器且指向真实元素，不能是 `end()`。
- `front/back` 只允许用于非空表。
- `*end()`、`++end()`、`--begin()` 不合法；只有非空表才允许 `--end()`。
- 内部结构即便是循环的，对外迭代也不是“无限绕圈”。
- 默认构造的 iterator 是未绑定位置，不能用来访问、移动或修改容器；复制它不赋予有效位置。
- 不要求检测跨容器位置、悬空 iterator 等误用。可以用调试断言辅助发现部分错误，但不保证诊断所有非法调用。
- 元素析构按不抛异常处理。本周不设计抛异常析构的恢复机制。

这些边界属于契约，不是让测试随意执行未定义行为。标准 iterator 抽象也区分可解引用位置与尾后位置，详见 [iterator 通用要求](https://eel.is/c++draft/iterator.requirements.general)。

### 1.4 本周范围就此冻结

必须：上述需求、可修改 iterator、基础 const 成员函数、RAII、禁止不支持的复制移动、单元素插入失败不改变原表。

明确不做：完整 `const_iterator`、深拷贝、移动实现、allocator、`emplace`、仅可移动类型支持、完整标准 iterator 兼容、完整异常安全体系、`sort/merge/splice`、范围构造、initializer list、随机访问和性能 benchmark。

如果提前完成，先完成脱离代码验收，不自动新增功能。

---

## 2. 接口文件示范：用于讨论，不是最终实现

这份示范回答“对外可以怎样表达需求”，故意不替你确定节点结构、哨兵、成员布局和指针操作。阅读时把不懂的签名圈出来；不要求第一次就独立发明所有 C++ 语法。

建议你最终交付两份对应材料：

- `interface.md`：接口语义、复杂度、设计理由与不变量。
- `my_list.hpp`：你自己整理的声明和后续实现。模板实现本周放在头文件内，避免把精力消耗在模板分离编译问题上。

### 2.1 公开声明示范

下面只是接口示范，不是可以直接运行的完整容器。iterator 和容器的私有状态及函数定义均需后补。不要把此声明与自己的完整类定义同时放进一个翻译单元。

```cpp
#pragma once
#include <cstddef>

template<class T>
class List {
public:
    using value_type = T;
    using size_type = std::size_t;

    class iterator {
    public:
        iterator();  // 未绑定状态；不能解引用或移动

        T& operator*() const;
        iterator& operator++();    // 前置：返回移动后的自身
        iterator operator++(int);  // 后置：返回移动前的位置副本
        iterator& operator--();
        iterator operator--(int);

        bool operator==(const iterator& other) const;
        bool operator!=(const iterator& other) const;

    private:
        // TODO：保存位置；允许 List 创建代表合法位置的 iterator。
        // 不向普通调用者开放节点指针，也不拥有节点。
    };

    List();
    ~List();

    List(const List&) = delete;
    List& operator=(const List&) = delete;
    List(List&&) = delete;
    List& operator=(List&&) = delete;

    bool empty() const;
    size_type size() const;

    T& front();
    const T& front() const;
    T& back();
    const T& back() const;

    iterator begin();
    iterator end();

    iterator insert(iterator pos, const T& value);
    iterator erase(iterator pos);
    void push_front(const T& value);
    void push_back(const T& value);
    void clear();

private:
    // TODO：节点、容器状态、链接辅助函数和调试检查。
};
```

示范中的 iterator 可以复制和赋值，这与禁止复制容器不矛盾：前者复制位置，后者涉及节点所有权。最终实现仍应保证这一点。

这里没有 `begin() const`：在不实现 `const_iterator` 的范围内，不能从 const 容器返回允许修改元素的普通 iterator。本周 const 容器只支持相应只读查询和首尾访问。不要求 `operator->`、标准 iterator 类型标签或与所有标准算法互操作。

`T& operator*() const` 中，`const` 约束 iterator 自身的状态，不把所指元素变成 const。若这一点不清楚，作为讨论题处理，不靠背签名通过。

### 2.2 用一个外部例子检查语义

这个例子不是最终测试，只帮助你在接口讨论阶段确认调用者会如何使用容器。

```cpp
List<int> xs;
xs.push_back(10);
xs.push_back(30);

auto pos = xs.begin();
++pos;                       // 指向 30
auto added = xs.insert(pos, 20);
// 序列：10, 20, 30；*added == 20；*pos 仍是 30

auto next = xs.erase(added);
// 序列：10, 30；next == pos；added 已失效，不能继续使用

auto last = xs.end();
--last;                      // 非空，因此合法；*last == 30
```

### 2.3 你自己的接口文档填写框架

不用写长报告。接口表写全，设计理由每项几句就够。

```markdown
# List 接口与设计 v0.1

## 外部承诺
对外是什么：
本周不支持什么：
元素 T 的要求：

## 接口表
| 签名 | 含义/返回值 | 前置条件 | 复杂度 | iterator/引用有效性 |
| --- | --- | --- | --- | --- |

## 内部表示选择
我的表示：
它分别满足哪些需求：
考虑过但未采用的一个替代表示：
替代表示的代价或不满足的条件：

## 三张结构图
空表 / 单元素 / 三元素；标出位置、链接与数量。

## 不变量与所有权
结构条件：
空表条件：
数量条件：
所有权与生命周期：

## 待讨论问题
我不理解的签名或语义：
我不能证明正确的设计：
```

接口文档不是一次定稿后永远不能改。发现问题可以修订，但要同步文档、实现和测试，注明改变了哪条语义。

---

## 3. 如何把需求推到内部表示

这不是先写一堆设计术语，再照抄一种结构。实际只做三步：

1. 从需求表选出最影响表示的约束：双向移动、给定位置插删 O(1)、两端访问 O(1)、位置稳定。
2. 画一个候选表示，逐条检查能否满足；不满足就补信息或换表示。
3. 记录一个替代方案以及不选择它的原因，然后停止枚举，进入实现。

**推荐候选是循环双向链表加哨兵与数量字段，但这不是标准强制布局，也不是唯一满足需求的结构。** 非循环双链配首尾指针同样可以满足许多要求，只是边界处理不同。你要能解释“选它减少了哪些特判”，而不是说“不这样就不标准”。

若选循环双向哨兵，检查这些问题：

- 空表如何让前后链接一致？
- `begin/end` 分别对应哪里？
- 为何内部可循环，外部却禁止 `++end()`？
- 哨兵不代表元素，如何避免创建一个无意义的 T？可与 AI 讨论“链接基类与元素节点分离”等方案，不必独立发明。
- 每条相邻关系是否能反向走回来？从起点遍历是否回到预期边界？实际元素数量是否等于保存的数量？

**不变量约束的是可观察的稳定状态：构造完成、操作入口和正常完成出口，以及承诺恢复原状的失败出口。** 多条指针赋值的中间瞬间，部分关系可能暂时不成立；本周是单线程实现，不要求每一行之后所有不变量都成立。

调试检查器用于检查选定表示，而不是替代内存检查。它可能通过断言检查链接互逆、边界与计数；遍历要有步数上限以免错误成环后无限挂住。它无法安全识别任意野指针，失效内存仍需 sanitizer 等工具辅助。

---

## 4. 四个时段的子任务、产物与阶段闭环

### 总览：本周沿这一条主线推进

| 时段 | 主攻方向 | 可留下的产物 | 进入下一阶段的依据 |
| --- | --- | --- | --- |
| 周二课，90 分钟 | 外部需求 → 接口 → 内部表示；建立可编译起点 | 接口草案、三张图、空表骨架 | 能解释表示如何满足主要需求；空表可构造和销毁 |
| 周三晚，50 分钟 | 推导并实现最小结构变化 | 插入/删除图、核心实现或明确失败案例 | 能解释修改哪些边；最小增删过程可检查 |
| 周四课，90 分钟 | 核心结构 → 可用容器；准备测试交付 | 公开接口实现、最小使用示例 | iterator、所有权和 API 能协同工作，可交给 AI 测试 |
| 周末，余量内 | 外部测试 → 修正 → 学习验收 | 测试记录、必要修复、脱离代码复述 | 工程验证与学习证据同时达标 |

这些是目标产物，不是对完成速度的预测。没通过阶段出口，就把未完成项顺延；不跳过所有权或验证来追赶日期。

### 4.1 周二：先知道要造什么，再搭起空容器

**本次回答的问题：这组外部需求，为什么需要我选的内部表示？**

- [ ] 阅读需求表和接口示范，圈出真正不懂的地方，不先抄实现。
- [ ] 写自己的 API 声明与语义表；至少说明位置含义、返回值、前置条件和复杂度。
- [ ] 画空表、单元素、三元素图；写出关键不变量和节点所有权。
- [ ] 给出一个候选表示和一个替代方案，说明选择理由。
- [ ] 把草案发给 AI 讨论。可以直接问具体语法，不需要为了“独立”卡在模板或运算符声明上。
- [ ] 在核心语义清楚后，搭建 `my_list.hpp` 和最小 `main.cpp`：先让空容器构造、查询、析构可编译运行；尚未调用的成员可以暂只声明。

产物：`interface.md` 初稿、图、能够实际编译的空表起点。若讨论尚未完成，留下未决问题，不假装已经定稿。

阶段闭环：不看代码，说明空表的内部状态，以及 `begin == end` 为什么成立；指出双向移动和给定位置删除分别需要什么内部信息。

交给 AI 的信息：“这是接口草案、结构图及我不确定的点。请检查语义、复杂度与表示是否一致，暂不写完整实现。”

### 4.2 周三：从图推出一次插入和一次删除

**本次回答的问题：操作前后哪些边改变，为什么不会丢节点或破坏边界？**

- [ ] 接续周二未完成的设计决定，不重开所有方案。
- [ ] 手画“在某位置前插入”和“删除某元素”的前后图；列出需要保留的位置和需要改变的链接。
- [ ] 写首次核心修改代码；分清创建节点、链接、摘除、销毁各自的责任。
- [ ] 修改完成后检查结构与数量；特别观察从空表到一个元素，再删除回空表。
- [ ] 做少量运行检查：空表插入、再追加一个、删除唯一元素或两端元素。只为继续开发提供反馈，不编写系统测试框架。

**避免实现顺序死锁：** `insert/erase` 接口使用 iterator，不代表必须先写完全部 iterator 运算符。可先实现其最小位置包装、容器创建位置所需的访问关系，再逐步补 `*`、`++`、比较；也可以先验证私有链接原语。内部辅助代码不必公开裸节点。

产物：自己推导的两张操作图，以及核心代码的当前状态。若只能完成插入，明确记录删除尚未完成，周四先接着做。

阶段闭环：能对应自己的代码指出“这一处改的是图中的哪条边”；已完成的最小操作后结构可检查，销毁已有元素不泄漏。不要继续堆接口掩盖断链。

遇到错误交给 AI：“最小操作序列是……预期是……实际是……这是图与代码。请先指出可能破坏的不变量，优先给定位提示。”

### 4.3 周四：让内部结构成为对外可使用的容器

**本次回答的问题：调用者不碰节点，能否完成需求？多个位置与节点生命周期是否互不混淆？**

- [ ] 先补完尚不稳定的插入、删除及释放，不带着已知结构错误扩展。
- [ ] 补齐 iterator 的解引用、前后移动、比较；检查前置和后置返回语义。
- [ ] 接上 `begin/end`，试两个 iterator 独立移动；非空时从 `end` 向前遍历。
- [ ] 完成首尾访问、头尾插入、状态查询，复用通用结构逻辑。
- [ ] 完成 `clear`、析构、禁止复制移动；检查插入先成功构造再改链，且失败时不泄漏。
- [ ] 用 `int` 和 `std::string` 做简单调用；非默认构造类型等系统覆盖交给 AI。
- [ ] 对照接口文档勾选需求，不临时增加深拷贝、const_iterator 等支线。
- [ ] 提交代码、接口文档、最小示例、编译器/系统信息和已知问题，让 AI 生成适配测试。

产物：可供测试的 `my_list.hpp`、同步的 `interface.md`、最小 `main.cpp`。头文件里的临时未实现成员需标明，不能把完整声明当作已实现。

阶段闭环：只使用公开 API 跑通第 2.2 节的例子；两个 iterator 的移动互不干扰；容器离开作用域会释放已有节点。能解释“复制位置”和“复制容器”为何不是一回事。

如果代码尚未就绪，就提交部分实现与明确缺口，AI 可以先做局部测试；不会把尚未实现的需求判作已完成。

### 4.4 周末：运行 AI 测试，再验收是否真正学会

**本次回答的问题：实现满足契约吗？通过测试之后，我是否仍能独立重建关键推理？**

- [ ] 让 AI 根据最终接口提供测试源文件、适合当前环境的编译运行命令、预期成功输出。
- [ ] 自己运行，返回完整报错、失败用例或 sanitizer 输出；不要只转述“没过”。
- [ ] 由 AI 缩小失败序列、定位问题类别；结构修改由自己先尝试修正，语法和测试适配可交给 AI。
- [ ] 修复后重跑相关用例及全套测试，记录实际运行环境与结果。
- [ ] 关闭成品代码，完成第 6 节的学习验收。
- [ ] 写一段简短交付说明：满足了什么、缺什么、验证过什么、目前能独立解释什么。

产物：测试结果、修复后的实现、简短闭环记录。AI 没有看到代码或运行结果前，不应宣称你的实现已经合格。

阶段闭环：第 6 节所有必要证据成立。若周末余量不够，记录“本周未闭环 + 最小剩余问题”；不强行扩大工时，也不降低验收标准。

---

## 5. AI 与我的分工：测试交给 AI，设计责任仍在我

| 工作 | 我负责 | AI 负责 |
| --- | --- | --- |
| 需求与接口 | 理解需求、写自己的最终声明和解释、选择表示 | 示范、解释语法与语义、检查冲突 |
| 指针操作 | 画图、首次推导和实现、解释修改理由 | 提示破坏的不变量、给最小反例、审查 |
| C++ 工程语义 | 理解所有权、复制策略与 iterator 的区别 | 解释 RAII、模板、访问控制及错误信息；必要时给局部示例 |
| 开发反馈 | 做少量可运行调用，确认当前模块没有明显问题 | 根据当前实现提供必要的辅助检查 |
| 系统测试 | 提供最终接口/代码/环境；运行、反馈、修正 | 编写边界、差分、随机、生命周期测试及运行说明 |
| 学习验收 | 脱离代码解释和重建 | 按目标提问、核对证据，不以测试通过代替学习 |

你不需要独立设计随机测试器或背 sanitizer 参数；但必须读懂失败用例做了什么、违反哪条契约。否则修正仍只能依赖 AI。

### 交给 AI 的测试请求模板

> 这是我的 List 接口文档、实现、最小示例及系统/编译器信息。请按任务书提供可直接编译运行的测试文件，分别报告功能、生命周期与内存检查结果。不要测试我明确未承诺的功能，也不要用非法操作制造“必然报错”的要求。先给测试和运行方式，失败后优先给最小复现与定位提示，不直接重写我的整个容器。

### AI 测试应覆盖什么

| 测试类别 | 要查的承诺 |
| --- | --- |
| 空表、单元素、首尾与中间操作 | 序列、返回 iterator、边界及 size 正确 |
| 正反向遍历、多 iterator、通过引用修改值 | 遍历状态独立、元素访问语义正确 |
| 插入/删除后的存活 iterator 与引用 | 未删元素的位置和地址保持有效 |
| clear、重复构造销毁、生命周期计数类型 | 元素恰当创建销毁，无遗漏或重复析构 |
| 非默认构造但可复制类型、string | 泛型不依赖 int，不因哨兵要求默认构造 |
| 可控复制抛异常的类型 | 单元素插入失败后内容、数量和存活对象数不变 |
| 与 std::list 的合法操作差分、固定随机种子 | 序列行为相同，失败可重现 |
| 内部断言、内存检查工具 | 结构与生命周期问题得到额外检查 |
| 编译期检查或独立负向编译用例 | 不支持的复制和移动确实被禁止 |

不要解引用已经失效的 iterator 来“证明它失效”；那本身就是非法行为。测试应检查存活位置的稳定性，并在文档中确认失效位置不能使用。

自动测试不能证明所有输入都正确，也不能只凭运行时间证明 O(1)。复杂度需结合代码审查：给定位置的修改是否遍历，数量查询是否直接读取状态。调试版每次做 O(n) 结构检查不改变核心原语的分析，但要明确调试运行有额外成本。

内存工具按实际环境确定：支持时使用 AddressSanitizer / UndefinedBehaviorSanitizer，并确认泄漏检测确实启用且受该平台支持。不把“编译成功”或“没看到报错”自动等同于完成泄漏检查；工具不可用时由 AI 给出可用替代，并记录覆盖缺口。

---

## 6. 最终验收：两条线都完成才停止

### 6.1 工程线：我的容器是否符合自己承诺的契约

- [ ] R1–R12 已对应实现或验收证据，不以声明代替实现。
- [ ] AI 提供的约定测试已在明确环境运行，无未解释失败。
- [ ] 结构断言及适用的内存/泄漏检查已运行，结果无未解决问题。
- [ ] 文档和代码一致，非法操作边界明确，未实现能力明确禁止或未提供。

### 6.2 学习线：我是否能独立解释并重建

关闭成品代码，可以看自己的需求和接口文档，可以查 C++ 语法，但不能复制现成的指针逻辑。

| 验收动作 | 通过证据 |
| --- | --- |
| 画空表、单元素、三元素结构 | 标出真实元素、边界、begin/end、数量和所有权；图符合自己的不变量 |
| 推导核心修改 | 从图推出一次插入、一次删除，再检查“删除唯一元素”是否成立 |
| 空白重建 | 重写核心链接/摘除代码，解释每处修改；放入自己的实现副本后复跑相关测试。无需重写全部模板样板 |
| 解释设计 | 说清一条“需求 → 表示”的因果关系；比较一个替代表示；不把推荐方案说成唯一标准 |
| 解释边界 | 说明多个 iterator 为什么独立、end 为何不可解引用、节点何时释放、默认复制容器为何危险 |
| 解释复杂度 | 区分给定位置插入和先查找再插入；指出代码中的遍历在哪里 |
| 对照 std::list | 说明自己保留的核心语义及省略的 const_iterator、复制移动、allocator 等，不冒充完整标准容器 |

不要求一次说得毫无停顿。发现错误，能依据图、不变量或测试自行修正，也是能力证据；如果核心指针修改仍需照着 AI 的答案抄，则这部分尚未闭环。

最后留下这段记录即可，不另写长报告：

```markdown
## 本周交付记录
我选择的表示及原因：
我现在能独立重建的核心：
一次真实错误、违反的不变量及修复：
（若没有遇到，不编造，记录实际验证即可。）
测试环境、执行命令、结果：
与完整 std::list 的差距：
未完成或仍需帮助的部分：
状态：已闭环 / 未闭环
若未闭环，最小剩余动作：
```

---

## 7. 卡住时，如何继续而不走偏

- **接口语法不懂**：带着一个具体签名来问 AI；语法可以被示范，不必从零发明。
- **不知道怎么选表示**：挑最关键的一条需求，用两个候选图比较，不研究所有链表变体。
- **模板细节盖过结构**：可以临时用 int 做局部结构实验，但最终 R10 仍需泛型验证；临时降维不等于验收降标。
- **指针操作错了**：缩短到最小失败序列，先画操作前后，再对应代码查链接和生命周期。
- **课程结束时没做完**：留下“最后通过的调用、当前失败、下一项最小动作”，下次直接接续。
- **超出周预算**：停止加功能，提交当前产物与缺口。不能为了本周完成而跳过内存释放、测试或独立重建。

本周结束时，最值得留下的不是一份看起来像标准库的源码，而是一份你能解释、验证，并在忘记细节后重新推导出核心的实现。
